% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_zenodo.R
\name{get_ground_truth}
\alias{get_ground_truth}
\title{Get ground truth simulation data for a SCOUT SPN}
\usage{
get_ground_truth(
  spn,
  record_id = .scout_record_id(spn),
  cache_dir = .scout_cache_dir(spn)
)
}
\arguments{
\item{spn}{SPN identifier, e.g. \code{"SPN01"}.}

\item{record_id}{Zenodo record ID for this SPN.}

\item{cache_dir}{Local directory to cache downloaded files.
Defaults to \verb{~/.cache/SCOUT/<spn>/}.}
}
\value{
A named list of R objects, one per RDS file in the archive.
}
\description{
Downloads and unzips the ground truth archive from the SPN's Zenodo record
(if not already cached), then reads all RDS files into a named list.
}
\examples{
\dontrun{
gt <- get_ground_truth("SPN01", record_id = "1234567")
}
}
